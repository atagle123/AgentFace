name: agent_face
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - conda-build
  - graphviz  # System graphviz package
  - pip:
      - gradio[mcp]
      - smolagents[mcp]
      - fastmcp
      - transformers
      - graphviz